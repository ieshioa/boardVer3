<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.boardver3.user.UserMapper">

    <insert id="postUser">    <!-- 회원가입 -->
        INSERT user
        SET uid = #{uid}
        , upw = #{upw}
        , nm = #{nm}
    </insert>

    <select id="getUser">    <!-- 유저 정보 가져오기 -->
        SELECT user_id AS userId, uid, upw, nm, created_at AS createdAt, updated_at AS updatedAt
        FROM user
        WHERE uid = #{uid}
    </select>

    <update id="patchPassword">    <!-- 비밀번호 수정 -->
        UPDATE user
        SET upw = #{newPw}
        WHERE user_id = #{userId}
    </update>


</mapper>